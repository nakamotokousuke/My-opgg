import { PlayerData } from "./PlayerType";

export type BuildPlayerType = {
  puuid: string;
  perks: any;
  champion: string;
  index: number;
  matchId: string;
  // Player: PlayerData;
  participantID: number;
};
