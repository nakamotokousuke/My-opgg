export type PlayerData = {
  id: string;
  accoungId: string;
  puuid: string;
  name: string;
  profileIconId: number;
  revisionDate: number;
  summonerLevel: number;
  platform?: string;
  region?: string;
};
